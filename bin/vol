#!/bin/bash

case $1 in
    "up")
        pamixer -u; pamixer -i 5
        ;;
    "down")
        pamixer -u; pamixer -d 5
        ;;
    "mute")
        pamixer -t
        ;;
    "get")
        pamixer --get-volume
        ;;
esac
