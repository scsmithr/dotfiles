# -*- mode: snippet -*-
# name: React functional component boilerplate with styles
# key: fcs
# --
import React from "react"
import { makeStyles } from "@material-ui/core"

const useStyles = makeStyles({ wrapper:{} })

interface $1Props {}

/**
 * $0
 */
export const $1: React.FC<$1Props> = ({}) => {
  const styles = useStyles()
  return (<div className={styles.wrapper}><span>$1</span></div>)
}