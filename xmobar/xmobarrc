Config {
    font = "xft:Source Code Pro Semibold:size=11:antialias=true",
    bgColor = "#282c34",
    fgColor = "#abb2bf",
    position = TopSize C 100 40,
    allDesktops = False,
    lowerOnStart = True,
    hideOnStart = False,
    persistent = True,

    commands = [
        Run Date "%a, %b %d %I:%M%P" "date" 10,

        Run Com "current_wifi.sh" [] "wifi" 100,

        Run Com "current_vol.sh" ["<fc=#e5c07b>(mute)</fc>"] "vol" 100,
     
        Run Battery [
            "--template" , "bat: <acstatus>",
            "--Low",    "20",        -- units: %
            "--High",   "80",        -- units: %
            "--low",    "#e06c75",
            
            "--", -- Battery-specific options.
            "-o", "<left>% (<timeleft>)", -- Discharging status.
            "-O", "<left>% <fc=#98c379>(charging)</fc>",              -- AC "on" status.
            "-i", "<left>% <fc=#61afef>(full)</fc>" -- Charged status.
        ] 100,

        Run StdinReader
    ],
    sepChar = "%",
    alignSep = "}{",
    template = " %StdinReader% }  %date%  { %wifi% - %vol% - %battery% "
}
