Config {
    font = "xft:Fira Sans:medium:size=10:antialias=true",
    bgColor = "#26272d",
    fgColor = "#ababb4",
    border = BottomB,
    borderColor = "#282828",
    borderWidth = 2,
    position = Top,
    allDesktops = False,
    lowerOnStart = True,
    hideOnStart = False,
    persistent = True,

    commands = [
        Run Date "%a, %b %d %I:%M%P" "date" 10,

        Run CommandReader "~/dotfiles/status/status.scm" "status",

        Run StdinReader
    ],
    sepChar = "%",
    alignSep = "}{",
    template = " %StdinReader% }  %date%  { %status%  "
}
