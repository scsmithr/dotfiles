Config {
    font = "xft:Source Code Pro:size=12:antialias=true",
    bgColor = "#282c32",
    fgColor = "#abb2bf",
    position = Top,
    allDesktops = False,

    commands = [
        Run Date "%l:%M %P" "date" 10,
        Run StdinReader
    ],
    sepChar = "%",
    alignSep = "}{",
    template = "%StdinReader% }{ %date% "
}
